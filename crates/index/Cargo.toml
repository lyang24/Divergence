[package]
name = "divergence-index"
version.workspace = true
edition.workspace = true

[dependencies]
divergence-core = { workspace = true }
rand = { workspace = true }
rand_xoshiro = { workspace = true }
